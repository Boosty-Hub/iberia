import { EsqueletoPortada } from '@/components/esqueletos'

/** Lo que se ve entre el clic y la página. Ver `components/esqueletos.tsx`. */
export default function Cargando() {
  return <EsqueletoPortada />
}
